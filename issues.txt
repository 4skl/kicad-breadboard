issues
- validation icons are not placed correctly on the board, maybe tie it to a relevant component? 
- the power rails are colored correctly and are subdivided into groups of five, which is correct, but the total length of the rail does not match the board length. add some spacing between the groups of five to make it match up. check the previous breadboard image i shared as an illustration
-place designators on component symbol, to allow visual comparison between schematic and breadboard
-dont draw V1 in the components list, if it cant be placed. limit it to being available on the binding posts.
-scaling? full screen looks a bit awkward
- opamps do not translate to component list. i use the generic opamp from kicad.

hotkeys:
wire hotkey = w
delete hotkey = d
return to select/move = esc
Display hotkey list at the bottom right, beneath the component list.

